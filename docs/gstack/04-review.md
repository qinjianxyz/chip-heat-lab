# 04 Review

## Review Checklist

- Rust tests cover physical monotonicity expectations for this simplified model.
- CLI reads from stdin or file and emits only JSON.
- SwiftUI app uses `RustRunner` and does not link solver internals.
- Site replay reads snapshot files.
- Public copy avoids excluded claim categories outside non-claim documents.

## Current Status

Ready for local verification with `bash scripts/verify.sh`.
